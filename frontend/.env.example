# Copy to .env.local for local `npm run dev` (Vite loads .env.local automatically
# and it's gitignored). Not yet wired into docker-compose.yml / nginx — see
# PROGRESS-LOG.md Phase 1 for why that's deferred.
VITE_API_BASE_URL=http://localhost:8080/api/v1
